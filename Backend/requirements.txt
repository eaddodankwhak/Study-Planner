Flask>=3.0,<4.0
gunicorn>=21.0,<24.0

# Production database: psycopg2 talks to a Postgres server (e.g. Neon) when
# DATABASE_URL is set. Without it the app falls back to local SQLite.
psycopg2-binary>=2.9.10,<3.0

# Optional: richer study-material text extraction. The AI Hub degrades gracefully
# when these are not installed (plain-text files still work everywhere).
PyPDF2>=3.0,<4.0
python-docx>=1.1,<2.0
Pillow>=10.0,<12.0