(function () { "use strict"; var questions=[].slice.call(document.querySelectorAll("[data-question]")); if(!questions.length)return; var index=0, timer=document.querySelector("#quiz-timer"), form=document.querySelector("#personal-quiz-form"); function draw(){questions.forEach(function(q,i){q.classList.toggle("is-current",i===index)});document.querySelector("[data-progress]").textContent=(index+1)+" / "+questions.length;document.querySelector("[data-previous]").disabled=index===0;document.querySelector("[data-next]").hidden=index===questions.length-1;document.querySelector("[data-submit]").hidden=index!==questions.length-1} document.querySelector("[data-next]").onclick=function(){index=Math.min(index+1,questions.length-1);draw()};document.querySelector("[data-previous]").onclick=function(){index=Math.max(index-1,0);draw()};var remaining=Number(timer.dataset.seconds);function tick(){var m=Math.floor(remaining/60),s=remaining%60;timer.textContent=m+":"+String(s).padStart(2,"0");if(remaining--<=0){form.submit();return}setTimeout(tick,1000)}draw();tick()}());
